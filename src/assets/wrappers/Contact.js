import styled from 'styled-components';

const Wrapper = styled.div`
    .contact-container {
        position: relative;
        width: 100%;
        min-height: 80vh;
        overflow: hidden;
        color: var(--black);
    }
`;

export default Wrapper;
